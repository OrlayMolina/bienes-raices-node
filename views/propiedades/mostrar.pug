extends ../layout/app

block contenido 

   //-- pre= JSON.stringify(propiedad, undefined, 2)

   div(class="py-5")
      h1(class="text-4xl my-8 font-extrabold text-center")= propiedad.titulo

      div
         a(href="#" class="font-bold text-gray-600 text-md") Categoria: 
            span(class="font-normal")= propiedad.categoria.nombre

      div(class="mt-5 md:flex md:gap-4")
         div(class="md:w-2/3")
            img(src=`uploads/${propiedad.imagen}` alt=`Imagen de la prodiedad ${prodiedad.titulo}`)
         div(class="md:w-1/3") 2