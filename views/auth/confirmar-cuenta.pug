extends ../layout/index

block contenido
    .py-5
        h1.text-4xl.my-10.font-extrabold.text-center.font-sans Bienes
            span.font-normal Raices
        h2.text-center.text-2xl.font-extrabold= pagina

    div( class="bg-white shadow-xl py-8 px-4 rounded-xl max-w-md mx-auto")

        p( class= error ? 'bg-red-500 rounded-xl' : 'bg-indigo-500 rounded-xl').py-2.px-5.rounded-xl-w-full.font-bold.text-center.text-white.uppercase= mensaje

        if !error
            a(href="/auth/login" class="text-center font-bold text-sm text-slate-600 uppercase mt-10 block") Ya puedes Iniciar Sesión